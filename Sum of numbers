#include<stdio.h>
#include<stdlib.h>
int main(){
    int num;
    scanf("%d",&num);
    int sum = 0;
    while (num>9){
        sum=sum+num%10;
        num=num/10;
    }
    sum=sum+num;
    printf("%d", sum);
}
